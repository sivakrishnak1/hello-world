# Query Dynatrace API for your service (replace with your credentials)
curl -X GET \
  "https://your-dynatrace-env.live.dynatrace.com/api/v2/entities?entitySelector=type(SERVICE),entityName.equals('YOUR_SERVICE_NAME')" \
  -H "Authorization: Api-Token $DT_API_TOKEN"
